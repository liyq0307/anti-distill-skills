# Layout
# 目录布局

- `profiles/` stores reusable rule presets.
- `profiles/` 保存可复用的规则预设。

- `templates/` stores reusable output templates.
- `templates/` 保存可复用的输出模板。

- `docs/` stores operator-facing explanations for modes and structure.
- `docs/` 保存面向操作者的模式与结构说明。
