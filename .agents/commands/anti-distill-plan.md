# /anti-distill-plan

Generate a defense plan before sanitization.

## Usage

`/anti-distill-plan <input_path> [mode]`

Defaults:
- `mode`: `sanitize`

## Execution

```powershell
python .\anti-distill\scripts\anti_distill.py plan --input <input_path> --mode <mode>
```
